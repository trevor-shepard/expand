{
  "name": "expand",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Expand — Conway puzzle game (modernized monorepo)",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently -n web,api -c cyan,magenta \"npm run dev -w @expand/web\" \"npm run dev -w @expand/server\"",
    "build": "npm run build -w @expand/contracts && npm run build -w @expand/game-engine && npm run build -w @expand/web && npm run build -w @expand/server",
    "start": "npm run start -w @expand/server",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:seed": "npm run db:seed -w @expand/server",
    "lint": "eslint .",
    "typecheck": "npm run build -w @expand/contracts && npm run build -w @expand/game-engine && npm run typecheck -ws --if-present",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@eslint/js": "^9.22.0",
    "@types/node": "^22.13.10",
    "concurrently": "^9.1.2",
    "drizzle-kit": "^0.31.11",
    "eslint": "^9.22.0",
    "tsx": "^4.23.15",
    "typescript": "^5.8.2",
    "typescript-eslint": "^8.26.1",
    "vitest": "^3.0.9"
  }
}
